<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record model="mail.channel" id="channel_for_approval_users">
            <field name="name">User Approval</field>
            <field name="group_public_id" ref="vixo_website_signup.group_approval_channel"/>
            <field name="group_ids" eval="[(4, ref('vixo_website_signup.group_approval_channel'))]"/>
            <field name="description">Notifications for user approval.</field>
        </record>

    </data>
</odoo>
